---
tags:
  - "#kb/Bash"
title: git解决冲突简短入门教程
---
入门级 - “救火与手动修复”

**核心目标： 识别冲突并手动修复，让合并能够完成。**

# 关键技能与理解：

如何引发冲突： 执行 git merge 或 git pull 时，如果看到 CONFLICT (content) 的提示，就知道冲突发生了。

识别冲突标记： Git 会在冲突文件中插入特殊的标记，让你清楚地看到不同版本的代码。

``` bash
<<<<<<< HEAD
这是你当前分支的代码
=======
这是你要合并过来的分支的代码
>>>>>>> branch-name
```

# 基本修复流程：

不要慌，冲突是正常现象。

## 使用 git status 查看哪些文件是 Unmerged（未合并）状态。

## 用文本编辑器（如 VSCode、Vim、Notepad++）打开这些文件。

手动决定要保留的代码：可能是保留一边，也可能是将两边的代码结合起来，或者完全重写。

删除冲突标记（<<<<<<<, =======, >>>>>>>），只留下你最终想要的代码。

保存文件。

## 完成合并

使用 git add <文件名> 将修复后的文件标记为“已解决”。

使用 git commit 来完成合并提交。Git 会为你生成一个默认的提交信息。

典型工具： 纯文本编辑器。